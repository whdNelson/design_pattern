
#server settings
server:
  port: 8080

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/design_pattern?useUnicode=yes&characterEncoding=UTF-8&serverTimezone=UTC"
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:../mapper/*Mapper.xml
  type-aliases-package: com.wanghaodong.design_pattern.model

#showSql
logging:
  level:
    com:
      example:
        mapper : debug

